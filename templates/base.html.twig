<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Your services assistant{% endblock %}</title>
        <link rel="icon" href="img/logo/cs-favicon.png">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header class="header">
            <img src="img/logo/cs-logo.png" alt="cs-logo.png" class="cs-logo">
            <div class="header-title">Your services assistant</div>
            <div class="header-user-action">
                <a href="auth">Sign In</a>
                <a href="registration">Sign Up</a>
            </div>
        </header>
        <content class="content">
            {% block body %}{% endblock %}
        </content>
        <footer class="footer">
            &copy; {{date}} ComService
        </footer>

    </body>
</html>
